<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no"
    />
    <title>AcneSense Responsive Menu</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Manrope:wght@700;800&family=Maven+Pro:wght@500&display=swap"
      rel="stylesheet"
    />
    <style>
      body,
      html {
        overscroll-behavior: none;
        -webkit-overflow-scrolling: touch;
        margin: 0;
        padding: 0;
        font-family: "Manrope", sans-serif;
      }
      .title-font {
        font-weight: 800;
        font-size: 1.75rem; /* 28px approx */
        user-select: none;
      }
      .accent-green {
        color: #40c057;
      }
      .sub-title-font {
        font-family: "Maven Pro", sans-serif;
        font-weight: 500;
        font-size: 0.875rem; /* 14px */
        user-select: none;
      }
      .icon-style {
        font-size: 1.25rem;
      }
      /* Rounded corner style for each button (similar radius on top-left) */
      .btn-rounded {
        border-top-left-radius: 1rem;
      }
    </style>
  </head>
  <body>
    <!-- Tampilan HP -->
    <div class="block md:hidden min-h-screen bg-white relative pb-20">
      <header class="bg-[#E5F6E5] px-6 pt-6 pb-4 flex items-center justify-between shadow-sm">
        <div>
          <h1 class="text-black title-font leading-tight select-none m-0 p-0">
            Acne<span class="accent-green">Sense</span>
          </h1>
          <p class="sub-title-font leading-normal m-0 p-0">
            For Your Acne Solution
          </p>
        </div>
        <div>
          <a href="/profile">
          <div class="w-10 h-10 rounded-full border border-gray-300 flex items-center justify-center overflow-hidden">
            <!-- Placeholder avatar circle -->
            <img src="<%= user.foto_profile ? user.foto_profile : '/image/foto_profile/default.png' %>" alt="avatar" />
          </div>
          </a>
        </div>
      </header>
      <main class="p-6 space-y-6 flex flex-col items-center">
        <!-- Button 1 -->
        <button onclick="window.location.href='/deteksi'"
          class="relative rounded-xl w-[317px] h-[129px] bg-[#2C5E5F] text-white p-6 select-none"
        >
          <i class="fa-solid fa-qrcode icon-style absolute top-6 left-6"></i>
          <i class="fa-solid fa-arrow-right icon-style absolute top-6 right-6"></i>
          <div class="absolute left-5 top-[70px] font-semibold text-lg">
            Foto dan deteksi sekarang!
          </div>
        </button>
      
        <!-- Button 2 -->
        <button onclick="window.location.href='/riwayat'"
          class="relative rounded-xl w-[317px] h-[129px] bg-[#FF8A65] text-white p-6 select-none"
        >
          <i class="fa-regular fa-file-lines icon-style absolute top-6 left-6"></i>
          <i class="fa-solid fa-arrow-right icon-style absolute top-6 right-6"></i>
          <div class="absolute left-5 top-[70px] font-semibold text-lg">
            Riwayat deteksi
          </div>
        </button>
      
        <!-- Button 3 -->
        <button onclick="window.location.href='/chatbot'"
          class="relative rounded-xl w-[317px] h-[129px] bg-[#484DA1] text-white p-6 select-none"
        >
          <i class="fa-solid fa-robot icon-style absolute top-6 left-6"></i>
          <i class="fa-solid fa-arrow-right icon-style absolute top-6 right-6"></i>
          <div class="absolute left-5 top-[70px] font-semibold text-lg">
            Mulai chat dengan AI
          </div>
        </button>
      </main>
      <footer class="fixed bottom-0 w-full flex items-center justify-center">
        <img src="Ellipse 3.png" alt="" />
      </footer>
    </div>

<!-- Desktop/Tablet version -->
<div class="hidden md:flex w-full h-screen mx-auto bg-[#D6EBD5] rounded-2xl overflow-hidden " >
  <div class="flex flex-1 items-center justify-center" style="margin: 0 20px;">
    <div
      class="bg-white w-full max-w-7xl rounded-2xl p-5 flex"
      style="min-height: 600px" 
    >
      <!-- Left Panel -->
   <%- include('templates/menu-desktop.ejs'); %>

      <!-- Right Panel -->
      <div class="w-2/3 flex flex-col rounded-r-2xl overflow-hidden">
        <header class="bg-[#6A9E61] p-6 flex items-center justify-between select-none">
          <h2 class="font-semibold text-black text-lg">Homepage</h2>
          <a href="/profile">
          <div class="w-12 h-12 rounded-full border border-gray-300 flex items-center justify-center overflow-hidden bg-white">
            <img src="<%= user.foto_profile ? user.foto_profile : '/image/foto_profile/default.png' %>" alt="avatar" />
          </div>
          </a>
        </header>
        <div
          class="flex-1 flex items-center justify-center bg-[#999999] text-black select-none font-semibold text-xl"
        >
          <i class="fa-solid fa-arrow-left icon-style mr-3"></i> Silahkan Pilih Menu
        </div>
      </div>
    </div>
  </div>
</div>
  </body>
</html>